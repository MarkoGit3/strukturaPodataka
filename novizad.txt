Iduci
Napisati program koji pomoću vezanih listi (stabala) predstavlja strukturu direktorija.
Omogućiti unos novih direktorija i pod-direktorija, ispis sadržaja direktorija i
povratak u prethodni direktorij. Točnije program treba preko menija simulirati
korištenje DOS naredbi: 1- "md", 2 - "cd dir", 3 - "cd..", 4 - "dir" i 5 – izlaz.

unos mreko md, cd ko linux, cd ... nazad, exit za van.
preko stabla, aka dvostruko vezani pokazivaci i stacka (u njemu pamtit prethodne da znamo gdje se trenutno nalazimo)
struct directory
char ime
directPosition subDir
directPosition next}
struct stack
drPosition dirLevel (razina hajrahije u kojoj smo)
structPosition next}
main:
headDir
headStack
alokacije za "C:"
oba na nulu mu pokazivaca, pa:
headDir-next=rootDir
pa, C direktorij dodat na stack
novi čvor newStack kopirat s dirrektorija u new stack
odnosno push adresu od headStack i vrijednost push(headStack, rootDir);
to je prvi direktorij.
switch case izbornik.
1) md
alocirat newEl
//stog govori di se generiraju novi direktoriji
npr Users pokazivaci na NULL
pa citamo roditelja aka pristupit stacku i dobit root dir kao pokazivac
odnosno (parentdirektorij) headStack-next-dirLevel 
sta je isto kao newEl-dirleve sta je rootDir
//ne treba sortiranje pri unosu za ovi zadatak
childEl-next = parentDir--subDir //podirektroija od C
parentDir-subDir = childEl
//unili tako prvi el, kad se novi unese smao se ponovi
//mislim da se svakako ubaciva na kraj koje vec razine
2) cd dir (pozicija)
-provjeriti postojanje
-if petlja za provjeru gleda stanje na rootu pretraziva se po stogu odnosno sve na toj razini
-temp=headStack-next-driectlevel  (dom tu je isto ko do adresa od  C) -subdir-name  (provjerit ime direktorija u koji se zelimo ubacit)
3) cd .. (nazad)
provjera jesmo li u root
pop(&headStack) //izbacit zadnji el iz stacka
temp=HEADstack--next
headStack--next=temp--next (temp next an Null)
free(temp)
4) dir (pregled sadrzaja odnosno "ls isti")
ne treba nužno za varijable razinu (preko rekurzije za pod djelove, rekurzija losa za memoriju)
printDir(DirPosition, current, int level //samo za ispis bitan koliko tab)
{ DirPosition currSubdir=NULL //djeca
if(current--subdirPosition=NULL) //jel dica razlicito od nula
currSubdir=current--subdirPosition; 
while(currentSubdir!=NULL){
printf ... subdDIr--name //ime subdirextorija npr x64
printDir--name;
printDir(subbSubdir, level)
surrsubDir=currentsubDir--next
}
}
5)
oslobodit memoriju, na najmanju razinu pa gore i headDir i headStack i stack
//nema fajlova odi sve u RAM
